#!/usr/bin/env bash
set -euo pipefail
source_path="$(readlink -f -- "${BASH_SOURCE[0]}")"
script_dir="$(cd -- "$(dirname -- "$source_path")" && pwd)"
windows_script="$(wslpath -w "$script_dir/devctl.ps1")"
exec pwsh.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File "$windows_script" "$@"
